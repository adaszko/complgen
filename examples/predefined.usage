# This file illustrates the use of predefined nonterminals.

cmd <PATH>;

# cmd <DIRECTORY>;

# Note that it's perfectly fine to override the predefined meaning of those variables with your own:

<DIRECTORY> ::= foo | bar;
